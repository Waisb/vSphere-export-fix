Скрипт для превращения "кривого" экспорта vSphere в "нормальный" .xls файл
usage: export_fixer.py [-h] [-i INPUT] [-o OUTPUT]
optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Указать свое название файла для обработки.
  -o OUTPUT, --output OUTPUT
                        Указать свое имя выходного файла.